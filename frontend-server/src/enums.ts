export enum LoadingStatuses {
    default = 'default',
    loading = 'loading',
    loaded = 'loaded'
}

export enum LoadingStatusesContent {
    default = 'сохранить',
    loading = 'сохраняем',
    loaded = '✓'
}